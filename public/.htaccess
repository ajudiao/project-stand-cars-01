<IfModule mod_rewrite.c>
RewriteEngine On

# Se o arquivo ou pasta existir, não redireciona
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redireciona tudo para index.php
RewriteRule ^ index.php [QSA,L]
</IfModule>